apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sandbox
bases:
- ../../base

resources:
- ./ingress.yaml

images:
- name: ghcr.io/konempty/temp
  newTag: sandbox

patchesStrategicMerge:
- patch.yaml
- deployment.yaml
